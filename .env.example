# GitHub token used server-side to fetch live profile stats via the GraphQL API.
# Public data only — a fine-grained PAT with no extra scopes is enough.
# Locally: copy this file to .env.local and fill in the value (or run: gh auth token).
# Production: set GITHUB_TOKEN in Vercel -> Project Settings -> Environment Variables.
GITHUB_TOKEN=
